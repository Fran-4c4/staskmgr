# setup.py (Versión mínima)
from setuptools import setup

# Esta versión ya no necesita la compleja lógica de parseo,
# ya que todos los metadatos y dependencias están en pyproject.toml y setup.cfg
if __name__ == '__main__':
    setup()